@using Microsoft.Extensions.Options;
@using coreDemo.Settings;
@model IEnumerable<coreDemo.Models.Cinema>

@inject IOptions<ConnectionOptions> options;

    <div class="container">
        <table class="table">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Name</th>
                    <th scope="col">Location</th>
                    <th scope="col">Capacity</th>
                    <th scope="col">操作</th>
                    <th>@options.Value.DefaultConnection</th>
                </tr>
            </thead>
            <tbody>
                @Html.DisplayForModel()
            </tbody>
        </table>

        <a asp-action="Add">添加</a>
        @await Component.InvokeAsync("MovieCount", new { cinemaId=1 })
        <vc:movie-count cinema-id="1">

        </vc:movie-count>
    </div>
    
